#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 * Return: 0
 */
int main(void)
{
	char words;

	for (words = 'a' ; words <= 'z' ; words++)
		putchar(words);

	putchar('\n');

	return (0);
}
,
